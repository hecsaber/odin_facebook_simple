<%= form_with model: @user, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name%>
  <%= f.label :profile %>
  <%= f.text_area :profile %>
  <%= f.label :avatar %>
  <%= f.file_field :avatar %>
  <%= f.submit 'Submit' %>
<% end %>